from typing import Dict, Any
class HiroPlanner:

    def apply_flow(self, job_description: str):

        return {
            "fit_analysis": None,
            "resume": None,
            "contact": None,
            "dm": None
        }


planner = HiroPlanner()
